import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gabiot.abuelitoApp',
  appName: 'AbuelitoApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
